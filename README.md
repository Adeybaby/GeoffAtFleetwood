# Geoff Thompson — single-page site

Static one-page site. No build step.

## Files
- `index.html` — the page (CSS is inlined)
- `DC-WEL-Geoff265_0.jpg` — portrait
- `robots.txt`

## Local preview
Open `index.html` directly in a browser, or serve the folder:

```
python -m http.server
```
